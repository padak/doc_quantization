# Direct dependencies only; transitive ones are resolved by pip so that a
# compatible set is installed for whichever Python version is in use.
# Do not replace this with `pip freeze` output: pinning transitive packages
# to versions from one interpreter breaks installation on older ones.
anthropic>=0.40
httpx>=0.27
python-dotenv>=1.0
tiktoken>=0.7

# Observability web app (document conversion is delegated to the
# doc_converter companion service - this app ships no converter)
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9

# Development
pytest>=8.0
